{
  "name": "memuvie",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build -- --optimization --aot",
    "start": "cd frontend && npm start"
  },
  "private": true
}

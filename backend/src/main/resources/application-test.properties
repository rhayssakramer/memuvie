spring.application.name=memuvie

# DATABASE - URL simples para teste
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/memuvie_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA simples
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Desabilitar flyway temporariamente
spring.flyway.enabled=false

# JWT
jwt.secret=${JWT_SECRET:mySuperSecretJWTKey123}
jwt.expiration=86400000

# Cloudinary
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dvauwroyx}
cloudinary.api_key=${CLOUDINARY_API_KEY:414834393456219}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:u9aQX-vUJeiv6xXRdErvybNCHm8}

# Server
server.port=${PORT:8080}

# Logging
logging.level.root=INFO
logging.level.cha_revelacao=DEBUG
logging.level.org.springframework.security=DEBUG
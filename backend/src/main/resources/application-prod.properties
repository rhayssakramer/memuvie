spring.application.name=memuvie

# DATABASE - Formato correto do PostgreSQL para Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3dtrc2li9vc73d87tag-a.oregon-postgres.render.com:5432/memuvie_db}
spring.datasource.username=${DB_USERNAME:memuvie_db_user}
spring.datasource.password=${DB_PASSWORD:98Z4x8fNW6hDGmskJyInlEDuOmQhrjKT}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuração específica do HikariCP para PostgreSQL - OTIMIZADA
spring.datasource.hikari.jdbc-url=${DATABASE_URL:jdbc:postgresql://dpg-d3dtrc2li9vc73d87tag-a.oregon-postgres.render.com:5432/memuvie_db}
spring.datasource.hikari.username=${DB_USERNAME:memuvie_db_user}
spring.datasource.hikari.password=${DB_PASSWORD:98Z4x8fNW6hDGmskJyInlEDuOmQhrjKT}
spring.datasource.hikari.driver-class-name=org.postgresql.Driver

# Configurações de Performance - Pool de Conexões
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000

# JPA Performance Optimizations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# JWT
jwt.secret=${JWT_SECRET:mySuperSecretJWTKememuvie_jwt_secret_key_2025_super_seguro_nao_compartilhar_123456789}
jwt.expiration=86400000

# Cloudinary
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dvauwroyx}
cloudinary.api_key=${CLOUDINARY_API_KEY:414834393456219}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:u9aQX-vUJeiv6xXRdErvybNCHm8}

# E-mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:memuviedigital@gmail.com}
spring.mail.password=${MAIL_PASSWORD:qntn tvcf hglp xpnl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Uploads
upload.path=/uploads/memuvie/

# Flyway - Desabilitado temporariamente para debug
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# Server configurado para usar a porta do Render - OTIMIZADO
server.port=${PORT:8080}
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml
server.compression.min-response-size=1024

# HTTP/2 Support
server.http2.enabled=true

# Configurações de Timeout
server.servlet.session.timeout=30m
server.connection-timeout=60s

# Logging configuration para produção
logging.level.root=INFO
logging.level.cha_revelacao=INFO
logging.level.org.springframework=WARN
logging.level.org.hibernate=WARN
